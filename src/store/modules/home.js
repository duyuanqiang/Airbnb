import { createSlice } from "@reduxjs/toolkit";

const homeSlice = createSlice({
    name:"home",
    initialState:{
        name:"wang"
    },
    reducers:{

    }
})

export default homeSlice.reducer